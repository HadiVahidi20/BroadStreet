# 🎯 راهنمای کامل پنل ادمین Broadstreet RFC

## 🔐 احراز هویت امن با Google Sign-In

این پنل ادمین از **Google Sign-In** برای احراز هویت استفاده می‌کند. این یعنی:
- ✅ بدون نیاز به رمز عبور
- ✅ امنیت بالا با OAuth 2.0 گوگل
- ✅ فقط ایمیل‌های مجاز می‌توانند وارد شوند
- ✅ احراز هویت دو مرحله‌ای (اگر در Gmail فعال باشد)

---

## 📋 فهرست مطالب

1. [نصب و راه‌اندازی](#نصب-و-راه‌اندازی)
2. [تنظیم Google OAuth](#تنظیم-google-oauth)
3. [پیکربندی Google Apps Script](#پیکربندی-google-apps-script)
4. [استفاده از پنل ادمین](#استفاده-از-پنل-ادمین)
5. [بخش‌های قابل مدیریت](#بخش‌های-قابل-مدیریت)
6. [امنیت](#امنیت)
7. [عیب‌یابی](#عیب‌یابی)

---

## 🚀 نصب و راه‌اندازی

### مرحله 1: کپی فایل‌ها

تمام فایل‌های زیر در پروژه شما وجود دارند:

```
broadstreet-rfc-website/
├── pages/
│   └── admin.html                 # صفحه اصلی پنل ادمین
├── css/
│   └── admin.css                  # استایل پنل ادمین
├── js/
│   └── admin.js                   # منطق و Google Sign-In
└── sheets-template/
    └── google-apps-script-admin-api.js  # اسکریپت گوگل با احراز هویت
```

### مرحله 2: آپلود فایل‌ها

تمام فایل‌ها را روی سرور یا Netlify آپلود کنید.

---

## 🔑 تنظیم Google OAuth

### قدم 1: ایجاد پروژه در Google Cloud Console

1. به [Google Cloud Console](https://console.cloud.google.com) بروید
2. روی **Select a project** کلیک کنید
3. روی **NEW PROJECT** کلیک کنید
4. نام پروژه را وارد کنید (مثلاً `Broadstreet RFC Admin`)
5. روی **CREATE** کلیک کنید

### قدم 2: تنظیم OAuth Consent Screen

1. در منوی سمت چپ: **APIs & Services → OAuth consent screen**
2. User type: **External** را انتخاب کنید
3. روی **CREATE** کلیک کنید
4. اطلاعات اپ را پر کنید:
   - App name: `Broadstreet RFC Admin`
   - User support email: ایمیل شما
   - Developer contact: ایمیل شما
5. روی **SAVE AND CONTINUE** کلیک کنید
6. در Scopes: روی **SAVE AND CONTINUE** کلیک کنید
7. در Test users: ایمیل‌های مجاز را اضافه کنید
8. روی **SAVE AND CONTINUE** کلیک کنید

### قدم 3: ایجاد OAuth Client ID

1. در منوی سمت چپ: **APIs & Services → Credentials**
2. روی **CREATE CREDENTIALS → OAuth client ID** کلیک کنید
3. Application type: **Web application**
4. نام: `Broadstreet Admin Panel`
5. **Authorized JavaScript origins** را اضافه کنید:
   ```
   https://your-domain.com
   http://localhost:3000
   ```
6. **Authorized redirect URIs** را اضافه کنید:
   ```
   https://your-domain.com/pages/admin.html
   ```
7. روی **CREATE** کلیک کنید
8. **Client ID** را کپی کنید

### قدم 4: اضافه کردن Client ID به کد

فایل `js/admin.js` را باز کنید و خط زیر را پیدا کنید:

```javascript
const GOOGLE_CLIENT_ID = 'YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com';
```

آن را با Client ID واقعی جایگزین کنید:

```javascript
const GOOGLE_CLIENT_ID = '123456789-abc123xyz.apps.googleusercontent.com';
```

---

## ⚙️ پیکربندی Google Apps Script

### قدم 1: باز کردن Google Sheet

1. به Google Sheet خود بروید که داده‌های سایت در آن ذخیره شده است
2. مطمئن شوید شیت شما تب‌های زیر را دارد:
   - `news` (اخبار)
   - `fixtures` (بازی‌ها)
   - `sponsors` (اسپانسرها)
   - `players` (بازیکنان)
   - `hero` (اسلایدر صفحه اصلی)
   - `standings` (جدول رده‌بندی)

### قدم 2: ایجاد Apps Script

1. در Google Sheet، به منوی **Extensions → Apps Script** بروید
2. یک فایل جدید ایجاد کنید و نام آن را `AdminAPI` بگذارید
3. محتوای فایل `google-apps-script-admin-api.js` را کپی کنید
4. آن را در ادیتور Apps Script پیست کنید

### قدم 3: تنظیم ایمیل‌های مجاز

در کد Apps Script، بخش `ALLOWED_EMAILS` را پیدا کنید:

```javascript
var CONFIG = {
  ALLOWED_EMAILS: [
    // "admin@broadstreetrfc.co.uk",
    // "your.email@gmail.com",
  ],
  // ...
};
```

ایمیل‌های افرادی که باید به پنل ادمین دسترسی داشته باشند را اضافه کنید:

```javascript
ALLOWED_EMAILS: [
  "admin@broadstreetrfc.co.uk",
  "john.smith@gmail.com",
  "manager@broadstreetrfc.co.uk",
],
```

⚠️ **مهم:** فقط ایمیل‌هایی که در این لیست هستند می‌توانند وارد شوند!

### قدم 4: Deploy کردن به عنوان Web App

1. روی دکمه **Deploy** کلیک کنید
2. **New deployment** را انتخاب کنید
3. نوع را **Web app** انتخاب کنید
4. تنظیمات زیر را انجام دهید:
   - **Execute as:** Me (ایمیل شما)
   - **Who has access:** Anyone (یا Anyone with the link)
5. روی **Deploy** کلیک کنید
6. اجازه‌های مورد نیاز را تایید کنید
7. **URL** را که به شما داده می‌شود کپی کنید

این URL چیزی شبیه این است:
```
https://script.google.com/macros/s/AKfycby.../exec
```

---

## 🔐 استفاده از پنل ادمین

### ورود به پنل

1. به آدرس `/pages/admin.html` بروید
2. Web App URL را وارد کنید
3. روی دکمه **Sign in with Google** کلیک کنید
4. حساب Google خود را انتخاب کنید
5. اگر ایمیل شما مجاز باشد، وارد پنل می‌شوید

### رابط کاربری پنل

پس از ورود، یک داشبورد کامل با موارد زیر خواهید دید:

#### Sidebar (نوار کناری)
- **News:** مدیریت اخبار
- **Fixtures:** مدیریت بازی‌ها و نتایج
- **Sponsors:** مدیریت اسپانسرها
- **Players:** مدیریت بازیکنان
- **Hero Carousel:** مدیریت اسلایدر صفحه اصلی
- **Standings:** نمایش جدول رده‌بندی (فقط خواندنی)

#### عملیات اصلی

**اضافه کردن (Add):**
1. روی دکمه **Add** در بالای هر بخش کلیک کنید
2. فرم را پر کنید
3. **Save** را بکلید
4. داده‌ها مستقیماً در Google Sheet ذخیره می‌شوند

**ویرایش (Edit):**
1. روی آیکون **قلم** در کنار هر آیتم کلیک کنید
2. تغییرات را اعمال کنید
3. **Save** را بکلید

**حذف (Delete):**
1. روی آیکون **سطل زباله** کلیک کنید
2. حذف را تایید کنید

---

## 📦 بخش‌های قابل مدیریت

### 📰 News (اخبار)

فیلدها:
- **Title:** عنوان خبر
- **Date:** تاریخ (مثال: `1 Feb 2026`)
- **Category:** دسته‌بندی (مثال: `Match Report`, `Club News`)
- **Excerpt:** خلاصه خبر
- **Image URL:** آدرس تصویر
- **Link URL:** لینک به صفحه کامل خبر
- **Featured:** اگر `true` باشد، در بالای صفحه نمایش داده می‌شود

### 📅 Fixtures (بازی‌ها)

فیلدها:
- **Date:** تاریخ بازی
- **Time:** ساعت بازی
- **Home Team:** تیم میزبان
- **Away Team:** تیم مهمان
- **Venue:** محل برگزاری
- **Competition:** نام مسابقات
- **Home Score / Away Score:** نتیجه بازی
- **Status:** وضعیت (`upcoming`, `completed`, `postponed`)
- **Home BP / Away BP:** امتیازهای بونوس

⚡ **نکته:** پس از ویرایش بازی‌ها، جدول رده‌بندی به طور خودکار محاسبه می‌شود.

### 💰 Sponsors (اسپانسرها)

فیلدها:
- **Name:** نام اسپانسر
- **Tier:** سطح (`title`, `premium`, `partner`)
- **Logo Path:** مسیر لوگو
- **Website Link:** لینک به سایت اسپانسر
- **Tagline:** شعار

### 👥 Players (بازیکنان)

فیلدها:
- **Name:** نام بازیکن
- **Position:** پست بازی
- **Jersey Number:** شماره پیراهن
- **Image URL:** عکس بازیکن
- **Height / Weight:** قد و وزن
- **Age:** سن
- **Caps:** تعداد بازی‌ها
- **Team:** تیم (مثال: `1st XV`)

### 🎭 Hero Carousel (اسلایدر صفحه اصلی)

فیلدها:
- **Title:** عنوان (از `|` برای جدا کردن خطوط استفاده کنید)
- **Subtitle:** زیرنویس
- **Background Image:** تصویر پس‌زمینه
- **CTA Button Text / Link:** دکمه اول
- **Secondary CTA Text / Link:** دکمه دوم

### 📊 Standings (جدول رده‌بندی)

این بخش **فقط خواندنی** است و به طور خودکار از داده‌های بخش Fixtures محاسبه می‌شود.

---

## 🔒 امنیت

### Google Sign-In امنیت

سیستم احراز هویت Google Sign-In امنیت بالایی دارد:

✅ **مزایا:**
- بدون رمز عبور = بدون خطر افشای رمز
- احراز هویت دو مرحله‌ای Google
- توکن‌های امضای رمزنگاری شده
- سیاست‌های امنیتی Google

### اضافه/حذف ادمین‌ها

برای تغییر افرادی که دسترسی دارند:

1. به Google Apps Script بروید
2. بخش `ALLOWED_EMAILS` را پیدا کنید
3. ایمیل‌ها را اضافه یا حذف کنید
4. دوباره Deploy کنید

```javascript
ALLOWED_EMAILS: [
  "admin@broadstreetrfc.co.uk",
  "new.admin@gmail.com",  // اضافه کردن ادمین جدید
],
```

### محافظت از URL

- URL پنل ادمین را به کسی ندهید
- می‌توانید از `.htaccess` یا authentication سمت سرور استفاده کنید
- Web App URL را محافظت کنید

### پشتیبان‌گیری

Google Sheets به طور خودکار تاریخچه تغییرات را نگه می‌دارد:
- **File → Version history → See version history**

---

## 🐛 عیب‌یابی

### مشکل 1: "Unauthorized" یا "Email not allowed"

**راه‌حل:**
- مطمئن شوید ایمیل شما در لیست `ALLOWED_EMAILS` است
- بررسی کنید URL درست وارد شده باشد
- مطمئن شوید Apps Script به درستی deploy شده است

### مشکل 2: "Sign-in popup blocked"

**راه‌حل:**
- مرورگر را اجازه دهید popup باز کند
- صفحه را رفرش کنید و دوباره تلاش کنید
- از یک مرورگر دیگر استفاده کنید

### مشکل 3: "Failed to load data"

**راه‌حل:**
- بررسی کنید تب‌های Google Sheet نام صحیح دارند
- مطمئن شوید ردیف اول شامل هدرها است
- Apps Script را دوباره deploy کنید

### مشکل 3: CORS یا Network Error

**راه‌حل:**
- مطمئن شوید "Who has access" در Apps Script روی "Anyone" است
- کش مرورگر را پاک کنید
- از یک مرورگر دیگر امتحان کنید

### مشکل 4: تغییرات ذخیره نمی‌شوند

**راه‌حل:**
- بررسی کنید Apps Script اجازه‌های لازم را دارد
- لاگ‌های Apps Script را چک کنید: **Executions**
- مطمئن شوید Sheet قفل نیست

### مشکل 5: Standings بروز نمی‌شود

**راه‌حل:**
- این جدول از فایل قدیمی `google-apps-script.js` بروزرسانی می‌شود
- اطمینان حاصل کنید که آن اسکریپت هم در Apps Script شما وجود دارد
- می‌توانید trigger تنظیم کنید تا خودکار اجرا شود

---

## 📝 نکات مهم

1. **همیشه پشتیبان بگیرید** قبل از تغییرات بزرگ
2. **لیست ایمیل‌ها** را به‌روز نگه دارید
3. **URL ادمین** را خصوصی نگه دارید
4. **تصاویر** را در پوشه `assets/` آپلود کنید
5. **فرمت تاریخ** را یکسان نگه دارید

---

## 🎉 استفاده موفق!

اکنون شما می‌توانید تمام محتوای سایت را به راحتی از طریق پنل ادمین مدیریت کنید.

با استفاده از Google Sign-In، امنیت حساب شما تضمین شده است.

اگر سوالی دارید یا به کمک نیاز دارید، به فایل‌های کد مراجعه کنید یا با توسعه‌دهنده تماس بگیرید.

---

## 📞 پشتیبانی

برای مشکلات فنی:
1. چک کنید Console مرورگر چه خطایی نشان می‌دهد (F12)
2. لاگ‌های Apps Script را بررسی کنید
3. از نسخه آزمایشی پشتیبان استفاده کنید

---

**نسخه:** 1.0  
**تاریخ:** فوریه 2026  
**سازگار با:** Google Sheets API v4
